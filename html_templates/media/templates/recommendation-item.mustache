<div class="item">
	<img src="{{album-art}}" alt="{{album}}" />
	<div class="info">
		<h1>{{title}}</h1>
		<h2>{{artist}}</h2>
	</div>
	<div class="interaction">
		<a href="#" class="add">Add to playlist</a>
		<a href="#" class="dontlike">I don't like this</a>
	</div>
</div>